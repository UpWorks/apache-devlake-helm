# secrets.yaml
apiVersion: v1
kind: Secret
metadata:
  name: devlake-secrets
type: Opaque
data:
  # Generate a strong password and base64-encode it (e.g., `echo -n "yourpassword" | base64`)
  postgres-password: eW91cnBhc3N3b3Jk # Replace with base64-encoded password
  # Generate a 128-character encryption secret and base64-encode it
  encryption-secret: <base64-encoded-128-char-secret> # Replace with your own